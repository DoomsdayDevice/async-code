# Файл примера переменных окружения для docker-compose (frontend)
# Скопируйте этот файл как ".env" в корень репозитория и заполните значения.

# Frontend (Next.js)
# Переехало в async-code-web/.env — см. пример там

# Backend (Flask) — приватные ключи Supabase
# Эти переменные читаются из server/.env и не нужны фронтенду
# Не добавляйте сюда SERVICE_ROLE_KEY
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

